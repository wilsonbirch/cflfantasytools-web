# Signs the session cookie. `openssl rand -hex 32`.
SESSION_SECRET=""

# Where the api lives. On Fly this is private networking, so tokens never
# traverse the public internet:
#   http://cflfantasytools-api-dev.internal:4000/graphql
API_GRAPHQL_URL="http://localhost:4000/graphql"
